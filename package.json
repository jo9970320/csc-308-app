{
  "name": "csc-308-app",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm --workspace packages/react-frontend run dev",
    "build": "npm --workspace packages/react-frontend run build",
    "preview": "npm --workspace packages/react-frontend run preview"
  }
}
